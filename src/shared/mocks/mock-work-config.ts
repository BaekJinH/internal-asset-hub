import type { WorkConfig } from '@/entities/work-config/model/work-config-types'
import { DEFAULT_WORK_CONFIG } from '@/shared/constants/workboard'

export const mockWorkConfig: WorkConfig = { ...DEFAULT_WORK_CONFIG }
