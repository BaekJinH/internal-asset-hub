# DevPilot 엔진 API base URL.
# Pass 1은 MockDevPilotTransport가 default라 비워두어도 `/devpilot`이 동작합니다.
# 엔진 GA 시 devpilot-service의 주입을 HttpDevPilotTransport로 교체하고 이 값을 채우세요.
# 실 `.env`는 생성하지 마세요 — 이 예시 파일만 유지(머신 종속·시크릿 위생·두 환경 충돌 방지).
VITE_DEVPILOT_API_BASE_URL=
